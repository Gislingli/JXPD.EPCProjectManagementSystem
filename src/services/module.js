import { baseUrl } from './baseUrl';

let Add = `${baseUrl}/ModuleManage/Add`;
let QueryModelByCode = `${baseUrl}/ModuleManage/ListRecursivelyByParenteModuleCode`;

export {
  Add,
  QueryModelByCode
}
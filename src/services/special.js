import { baseUrl } from './baseUrl';

let specialAdd = `${baseUrl}/Special/Add`;
let specialDelete = `${baseUrl}/Special/Delete`
let specialUpdate = `${baseUrl}/Special/Update`;
let specialList = `${baseUrl}/Special/List`;
let specialGetById = `${baseUrl}/Special/GetSpecialById`;

export{
    specialAdd,
    specialDelete,
    specialUpdate,
    specialList,
    specialGetById,
}